name: Geki
on:
  push:
    branches: [ "Geki" ]
  pull_request:
    branches: [ "Geki" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Geki!
