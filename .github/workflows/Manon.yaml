name: Manon
on:
  push:
    branches: [ "Manon" ]
  pull_request:
    branches: [ "Manon" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Manon!
