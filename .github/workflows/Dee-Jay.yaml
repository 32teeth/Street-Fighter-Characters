name: CI
on:
  push:
    branches: [ "Dee-Jay" ]
  pull_request:
    branches: [ "Dee-Jay" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Dee-Jay!
