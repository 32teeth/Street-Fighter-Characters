name: CI
on:
  push:
    branches: [ "Gill" ]
  pull_request:
    branches: [ "Gill" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Gill!
