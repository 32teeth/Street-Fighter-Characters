name: Jamie
on:
  push:
    branches: [ "Jamie" ]
  pull_request:
    branches: [ "Jamie" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Jamie!
