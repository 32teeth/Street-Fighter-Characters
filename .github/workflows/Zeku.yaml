name: CI
on:
  push:
    branches: [ "Zeku" ]
  pull_request:
    branches: [ "Zeku" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Zeku!
