name: Abigail
on:
  push:
    branches: [ "Abigail" ]
  pull_request:
    branches: [ "Abigail" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Abigail!
