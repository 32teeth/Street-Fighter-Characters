name: CI
on:
  push:
    branches: [ "Decapre" ]
  pull_request:
    branches: [ "Decapre" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Decapre!
