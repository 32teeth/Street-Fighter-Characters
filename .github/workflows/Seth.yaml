name: Seth
on:
  push:
    branches: [ "Seth" ]
  pull_request:
    branches: [ "Seth" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Seth!
