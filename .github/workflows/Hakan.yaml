name: CI
on:
  push:
    branches: [ "Hakan" ]
  pull_request:
    branches: [ "Hakan" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hakan!
