name: CI
on:
  push:
    branches: [ "Necro" ]
  pull_request:
    branches: [ "Necro" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Necro!
