name: Adon
on:
  push:
    branches: [ "Adon" ]
  pull_request:
    branches: [ "Adon" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Adon!
