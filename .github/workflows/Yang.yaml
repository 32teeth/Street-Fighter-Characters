name: Yang
on:
  push:
    branches: [ "Yang" ]
  pull_request:
    branches: [ "Yang" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Yang!
