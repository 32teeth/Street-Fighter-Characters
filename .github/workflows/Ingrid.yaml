name: CI
on:
  push:
    branches: [ "Ingrid" ]
  pull_request:
    branches: [ "Ingrid" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Ingrid!
