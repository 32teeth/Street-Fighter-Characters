name: CI
on:
  push:
    branches: [ "Nash" ]
  pull_request:
    branches: [ "Nash" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Nash!
