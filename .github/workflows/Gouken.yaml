name: Gouken
on:
  push:
    branches: [ "Gouken" ]
  pull_request:
    branches: [ "Gouken" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Gouken!
