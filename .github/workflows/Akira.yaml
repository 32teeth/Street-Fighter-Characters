name: CI
on:
  push:
    branches: [ "Akira" ]
  pull_request:
    branches: [ "Akira" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Akira!
