name: CI
on:
  push:
    branches: [ "Ken" ]
  pull_request:
    branches: [ "Ken" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Ken!
