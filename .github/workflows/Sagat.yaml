name: CI
on:
  push:
    branches: [ "Sagat" ]
  pull_request:
    branches: [ "Sagat" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Sagat!
