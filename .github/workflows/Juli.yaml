name: CI
on:
  push:
    branches: [ "Juli" ]
  pull_request:
    branches: [ "Juli" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Juli!
