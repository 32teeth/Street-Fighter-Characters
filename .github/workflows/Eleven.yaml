name: CI
on:
  push:
    branches: [ "Eleven" ]
  pull_request:
    branches: [ "Eleven" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Eleven!
