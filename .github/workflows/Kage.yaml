name: Kage
on:
  push:
    branches: [ "Kage" ]
  pull_request:
    branches: [ "Kage" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Kage!
