name: CI
on:
  push:
    branches: [ "Rolento" ]
  pull_request:
    branches: [ "Rolento" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Rolento!
