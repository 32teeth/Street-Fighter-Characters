name: Juni
on:
  push:
    branches: [ "Juni" ]
  pull_request:
    branches: [ "Juni" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Juni!
