name: CI
on:
  push:
    branches: [ "Menat" ]
  pull_request:
    branches: [ "Menat" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Menat!
