name: Sodom
on:
  push:
    branches: [ "Sodom" ]
  pull_request:
    branches: [ "Sodom" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Sodom!
