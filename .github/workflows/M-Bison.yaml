name: CI
on:
  push:
    branches: [ "M-Bison" ]
  pull_request:
    branches: [ "M-Bison" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo M-Bison!
