name: CI
on:
  push:
    branches: [ "Vega" ]
  pull_request:
    branches: [ "Vega" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Vega!
