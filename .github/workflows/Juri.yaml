name: Juri
on:
  push:
    branches: [ "Juri" ]
  pull_request:
    branches: [ "Juri" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Juri!
