name: Karin
on:
  push:
    branches: [ "Karin" ]
  pull_request:
    branches: [ "Karin" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Karin!
