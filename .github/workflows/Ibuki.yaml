name: CI
on:
  push:
    branches: [ "Ibuki" ]
  pull_request:
    branches: [ "Ibuki" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Ibuki!
